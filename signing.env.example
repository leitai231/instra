# Copy this file to `signing.env` and fill in a valid local codesigning identity.
# You can list identities with:
#   security find-identity -v -p codesigning

APP_IDENTITY="Apple Development: your-email@example.com (TEAMID1234)"
